{
  "name": "minions-openclaw",
  "version": "0.1.0",
  "private": true,
  "description": "Manage, monitor, and version-control your OpenClaw Gateway instances",
  "license": "MIT",
  "author": "Mehdi Nabhani <mehdi@the-mehdi.com> (https://the-mehdi.com)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm --filter @minions-openclaw/sdk run build && pnpm --filter @minions-openclaw/cli run build && pnpm --filter @minions-openclaw/docs run build && pnpm --filter @minions-openclaw/blog run build && pnpm --filter @minions-openclaw/web run build",
    "deploy:blog": "pnpm --filter @minions-openclaw/blog run build && netlify deploy --dir apps/blog/dist --prod",
    "deploy:docs": "pnpm --filter @minions-openclaw/docs run build && netlify deploy --dir apps/docs/dist --prod",
    "deploy:web": "pnpm --filter @minions-openclaw/web run build && netlify deploy --dir apps/web/dist --prod",
    "test": "pnpm --filter @minions-openclaw/sdk run test",
    "lint": "pnpm --filter @minions-openclaw/sdk run build && pnpm --filter @minions-openclaw/sdk run lint && pnpm --filter @minions-openclaw/cli run lint",
    "deploy": "pnpm run deploy:blog",
    "dev": "echo 'no dev'",
    "stop": "pkill -f 'node.*minions-openclaw' || true"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mxn2020/minions"
  },
  "keywords": [
    "minions",
    "ecosystem",
    "compliance"
  ],
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}