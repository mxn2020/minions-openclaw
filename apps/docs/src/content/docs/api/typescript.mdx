---
title: TypeScript API Reference
description: Full TypeScript API documentation
---

## InstanceManager

```typescript
import { InstanceManager } from '@minions-openclaw/core';
const manager = new InstanceManager();
const instance = await manager.register(name, url, token?);
const instances = await manager.list();
const latencyMs = await manager.ping(id);
await manager.remove(id);
```

## GatewayClient

```typescript
import { GatewayClient } from '@minions-openclaw/core';
const client = new GatewayClient(url, token?, devicePrivateKey?);
await client.openConnection();
const presence = await client.fetchPresence();
await client.close();
```
