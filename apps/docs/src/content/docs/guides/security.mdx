---
title: Security
description: Security considerations for Minions OpenClaw
---

## Device Authentication

OpenClaw Manager supports device-based authentication using RSA key pairs.

## Token Storage

Auth tokens are stored in `~/.openclaw-manager/data.json`. Protect this file:

```bash
chmod 600 ~/.openclaw-manager/data.json
```
